import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  content: {
    gap: 16,
  },
  title: {
    paddingRight: 40,
  },
  actions: {
    flexDirection: 'row',
    gap: 12,
  },
  action: {
    flex: 1,
  },
})
